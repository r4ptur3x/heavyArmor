# Generated Maps

This folder contains generated map outputs for the Heavy Armor map-generation system.

Planned outputs:

- CSV map exports
- generated sheet-compatible grids
- test maps

The initial generator should write generated map files into this folder using the map spec `name` field.
