# Mapgen Pipeline Steps

This folder contains the detailed step files for the Heavy Armor map-generation pipeline.

`mapgen/rules/generation-pipeline.json` is the source-of-truth index and loading sequence.

Each step file is intentionally small so individual steps can be edited without replacing the entire pipeline file.
